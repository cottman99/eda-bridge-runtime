{
  "schema_version": "eda-runtime.eval-case/v1",
  "case_id": "l4.ads-docs-evidence",
  "level": 4,
  "title": "ADS bounded API evidence retrieval",
  "variables": ["ADS_CONNECTION"],
  "prompt": "Use only EDA Bridge Runtime tools and connection '{{ADS_CONNECTION}}'. Use eda.read operation 'docs.query' exactly once for purpose 'Find bounded ADS API evidence' with the exact vendor payload {\"query\":\"create an ADS workspace and schematic design with Python\",\"limit\":3}. Runtime must verify missing safety metadata mechanically; do not call capabilities separately. Select the most relevant returned source_ref and use eda.read operation 'docs.get' exactly once for purpose 'Retrieve one bounded ADS API passage' with vendor payload {\"source_ref\":<selected source_ref>,\"focus\":\"workspace and schematic creation\",\"max_chars\":2000}. Omit result_view because the exact response pointers are not established; never guess them. Do not use eda.submit, shell, files, browser, GUI, mutation, launch, simulation, or customer data. Do not quote or count the passage yourself. Return only a compact JSON object with status='passed', source_count copied from query result_count, and excerpt_chars copied from get returned_chars.",
  "required_tools": {
    "eda.read": 2
  },
  "allowed_tools": [
    "eda.read"
  ],
  "expected_runtime": {
    "minimum": {
      "largest_response_payload_chars": 100
    }
  },
  "expected_final": {
    "equals": {
      "status": "passed"
    },
    "minimum": {
      "source_count": 1,
      "excerpt_chars": 1
    }
  },
  "budgets": {
    "max_tool_calls": 2,
    "max_wall_seconds": 75
  },
  "safety": {
    "mutation": "forbidden",
    "solve": "forbidden",
    "customer_data": "forbidden",
    "raw_document_text_in_result": "forbidden"
  }
}
