{
  "schema_version": "eda-runtime.eval-case/v1",
  "case_id": "l2.ads-session-status",
  "level": 2,
  "title": "ADS typed read through Runtime",
  "variables": ["ADS_CONNECTION"],
  "prompt": "Use exactly one EDA Bridge Runtime tool call on connection '{{ADS_CONNECTION}}': run operation 'session.status' through the Runtime read-only tool with an empty vendor payload for purpose 'Benchmark one-call ADS typed session read'. Request one result_view field named session_count using pointer '/data/bridge/sessions' and mode 'count'; use that projected fact rather than requesting the full session inventory. Runtime must verify missing safety metadata mechanically; do not call capabilities separately. Do not use shell, files, documentation, GUI, launch, shutdown, or mutation. Return a compact JSON object with status='passed' and session_count from the actual Bridge response.",
  "required_tools": {
    "eda.read": 1
  },
  "allowed_tools": [
    "eda.read"
  ],
  "expected_final": {
    "equals": {
      "status": "passed"
    },
    "minimum": {
      "session_count": 0
    }
  },
  "budgets": {
    "max_tool_calls": 1,
    "max_wall_seconds": 45
  },
  "safety": {
    "mutation": "forbidden",
    "solve": "forbidden",
    "customer_data": "forbidden"
  }
}
